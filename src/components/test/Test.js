import axios from "axios";
import { useState } from "react";
const Test = () => {
  console.log("hii");
  const [] = useState([]);

  const clickHandler = () => {
 
  };

  return (
    <div>
      
      <button onClick={() => clickHandler()}>button</button>
    </div>
  );
};

export default Test;
